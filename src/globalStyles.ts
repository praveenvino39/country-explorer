import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    screen: {
        flex: 1,
    },
    actionContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        paddingHorizontal: 16
    }
})